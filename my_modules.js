penjumlahan = (num1, num2) => {
  return `${num1} plus ${num2} is ${num1 + num2}`;
};
penggurangan = (num1, num2) => {
  return `${num1} minus ${num2} is ${num1 - num2}`;
};

module.exports = {
  penjumlahan,
  penggurangan,
};
